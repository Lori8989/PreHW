#include <stdio.h>
#include <string.h>
#include "function.h"
#define max 100000

int solver(int **ptr, int *sum, char *s)
{
    int num[max]={0};
    int len=strlen(*s);
    int i=0;
    int sum=0;
    while(i<len){
        if(*s[i]=='-'){
            scanf("%1d",&num[])
        }

    }
}
